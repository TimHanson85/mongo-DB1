extends layout

block content
  h1= title
  if error
    h3= error
  p here is your shortened URL 
        a(href='/redirect/'+url.shortened)=url.shortened
        p=url.clicks 
        p=url.last_click_time
       
