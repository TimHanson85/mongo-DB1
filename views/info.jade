extends layout

block content
  h1= title
  if error
    h3= error
  p here is your shortened URL 
        a(href=url.target)=url.shortened
        p your link has been clicked url.clicks times
       
